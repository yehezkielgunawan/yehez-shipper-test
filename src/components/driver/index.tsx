import { Text } from "@chakra-ui/layout";

import MainWrap from "components/wrapper/MainWrap";

const DriverComponent = () => {
  return (
    <MainWrap>
      <Text color="red">Driver</Text>
    </MainWrap>
  );
};

export default DriverComponent;
